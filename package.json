{
  "name": "lob-mcp",
  "version": "1.3.0",
  "description": "MCP server for the Lob.com API — 78 tools + 23 design-spec resources for safe, print-correct LLM-driven physical mail (preview/commit, idempotency, piece caps, bundled Lob design templates).",
  "type": "module",
  "bin": {
    "lob-mcp": "build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && chmod 755 build/index.js && node scripts/copy-spec-pdfs.mjs",
    "prepare": "npm run build",
    "typecheck": "tsc --noEmit",
    "inspector": "npx @modelcontextprotocol/inspector build/index.js",
    "start": "node build/index.js",
    "test": "node --test --import tsx 'tests/unit/**/*.test.ts'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "pdf-lib": "^1.17.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "lob",
    "lob-api",
    "direct-mail",
    "address-verification",
    "postcards",
    "letters",
    "checks",
    "self-mailers",
    "claude",
    "ai",
    "anthropic"
  ],
  "author": "Optimize Overseas",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/optimize-overseas/lob-mcp.git"
  },
  "homepage": "https://github.com/optimize-overseas/lob-mcp#readme",
  "bugs": {
    "url": "https://github.com/optimize-overseas/lob-mcp/issues"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
